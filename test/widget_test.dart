import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('SplashScreen renders without crashing', (
    WidgetTester tester,
  ) async {
    // Tests reales se añadirán en futuros tickets
    expect(true, isTrue);
  });
}
